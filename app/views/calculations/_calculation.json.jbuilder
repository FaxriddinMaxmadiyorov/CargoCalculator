json.extract! calculation, :id, :firstname, :lastname, :middlename, :phone_number, :email, :weight, :length, :width, :height, :dep_point, :dest_point, :created_at, :updated_at
json.url calculation_url(calculation, format: :json)
